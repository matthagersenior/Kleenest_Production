import { listMobileActivity } from '@kleenest/mobile-core';
import { router } from 'expo-router';
import { useEffect, useState } from 'react';
import { FlatList, Pressable, SafeAreaView, StyleSheet, Text, View } from 'react-native';
export default function ActivityScreen(){const [rows,setRows]=useState<any[]>([]),[message,setMessage]=useState('Loading activity…');async function load(){try{const data=await listMobileActivity();setRows(data);setMessage(data.length?'':'No activity yet.')}catch(error:any){setMessage(error?.message||'Activity could not be loaded.')}}useEffect(()=>{load()},[]);return <SafeAreaView style={s.safe}><View style={s.header}><Text style={s.eyebrow}>ACTIVITY</Text><Text style={s.title}>Your activity</Text>{message?<Text style={s.message}>{message}</Text>:null}</View><FlatList data={rows} keyExtractor={item=>String(item.id)} contentContainerStyle={s.list} renderItem={({item})=><Pressable style={s.card} disabled={!item.locationId} onPress={()=>item.locationId&&router.push(`/location/${String(item.locationId)}`)}><Text style={s.cardTitle}>{item.title}</Text>{item.location?.name?<Text style={s.location}>{item.location.name}</Text>:null}{item.detail?<Text style={s.meta}>{item.detail}</Text>:null}<Text style={s.meta}>{new Date(item.createdAt).toLocaleString()}</Text></Pressable>}/></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:'#f4f7f5'},header:{padding:22,gap:8},eyebrow:{fontSize:12,fontWeight:'800',letterSpacing:2,color:'#4d6658'},title:{fontSize:32,fontWeight:'800',color:'#14231b'},message:{color:'#53645a'},list:{padding:20,paddingTop:0,gap:12},card:{backgroundColor:'#fff',padding:18,borderRadius:20,borderWidth:1,borderColor:'#dde7e0'},cardTitle:{fontSize:18,fontWeight:'800',color:'#14231b'},location:{fontSize:15,fontWeight:'700',color:'#31473a',marginTop:6},meta:{color:'#65756b',marginTop:5}});
