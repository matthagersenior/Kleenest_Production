import * as Linking from 'expo-linking';
import { listMobileFavoriteLocations } from '@kleenest/mobile-core';
import { router } from 'expo-router';
import { useEffect, useState } from 'react';
import { FlatList, Pressable, SafeAreaView, StyleSheet, Text, View } from 'react-native';
const idOf=(item:any)=>String(item.location_id||item.id||'');
const navigateUrl=(item:any)=>`https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(`${item.latitude},${item.longitude}`)}&travelmode=driving`;
export default function SavedScreen(){const [rows,setRows]=useState<any[]>([]),[message,setMessage]=useState('Loading saved places…');async function load(){try{const data=await listMobileFavoriteLocations();setRows(data);setMessage(data.length?'':'No saved places yet. Save a restroom from Explore or its location page.')}catch(error:any){setMessage(error?.message||'Saved places could not be loaded.')}}useEffect(()=>{load()},[]);return <SafeAreaView style={s.safe}><View style={s.header}><Text style={s.eyebrow}>SAVED</Text><Text style={s.title}>Your restroom shortlist</Text><Text style={s.body}>Open details, navigate directly, or add a saved place to a multi-stop route.</Text>{message?<Text style={s.message}>{message}</Text>:null}</View><FlatList data={rows} keyExtractor={(item,index)=>idOf(item)||String(index)} contentContainerStyle={s.list} renderItem={({item})=>{const id=idOf(item),hasCoordinates=Number.isFinite(Number(item.latitude))&&Number.isFinite(Number(item.longitude));return <View style={s.card}><Text style={s.cardTitle}>{item.name||item.location_name||'Restroom location'}</Text><Text style={s.meta}>{[item.address,item.city,item.state].filter(Boolean).join(', ')||'Location details available'}</Text><View style={s.actions}><Pressable style={s.secondary} onPress={()=>router.push(`/location/${id}`)}><Text style={s.secondaryText}>Details</Text></Pressable>{hasCoordinates?<Pressable style={s.primary} onPress={()=>Linking.openURL(navigateUrl(item))}><Text style={s.primaryText}>Navigate</Text></Pressable>:null}<Pressable style={s.secondary} onPress={()=>router.push({pathname:'/route',params:{add:id}})}><Text style={s.secondaryText}>Add to route</Text></Pressable></View></View>}}/></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:'#f4f7f5'},header:{padding:22,gap:8},eyebrow:{fontSize:12,fontWeight:'800',letterSpacing:2,color:'#4d6658'},title:{fontSize:32,fontWeight:'900',color:'#14231b'},body:{fontSize:15,lineHeight:22,color:'#53645a'},message:{color:'#53645a',fontWeight:'700'},list:{padding:20,paddingTop:0,gap:12},card:{backgroundColor:'#fff',padding:18,borderRadius:20,borderWidth:1,borderColor:'#dde7e0',gap:7},cardTitle:{fontSize:18,fontWeight:'800',color:'#14231b'},meta:{color:'#65756b'},actions:{flexDirection:'row',gap:8,marginTop:8,flexWrap:'wrap'},primary:{backgroundColor:'#173d2b',padding:12,borderRadius:12},primaryText:{color:'#fff',fontWeight:'900'},secondary:{backgroundColor:'#edf3ef',padding:12,borderRadius:12},secondaryText:{fontWeight:'800',color:'#173d2b'}});
