import { getKleenestSupabaseClient } from '@kleenest/mobile-core';

const client=()=>getKleenestSupabaseClient();
export async function listSavedRoutePlans(limit=30){const{data,error}=await client().from('route_plans').select('id,name,start_lat,start_lng,end_lat,end_lng,distance_miles,estimated_minutes,route_geometry,created_at,updated_at').order('updated_at',{ascending:false}).limit(limit);if(error)throw error;return data||[];}
export async function listOfflinePacks(limit=30){const{data,error}=await client().from('offline_packs').select('id,pack_type,name,status,route_discovery_session_id,expires_at,created_at,updated_at').order('updated_at',{ascending:false}).limit(limit);if(error)throw error;return data||[];}
async function ensureRouteGeometry(routeId:string){const c=client();const{data:route,error:routeError}=await c.from('route_plans').select('id,start_lat,start_lng,end_lat,end_lng,route_geometry').eq('id',routeId).single();if(routeError)throw routeError;if(route?.route_geometry)return route.route_geometry;const{data:stops,error:stopsError}=await c.from('route_stops').select('stop_order,location_id,locations!inner(latitude,longitude)').eq('route_id',routeId).order('stop_order',{ascending:true});if(stopsError)throw stopsError;const points:[[number,number],...Array<[number,number]>]=[[Number(route.start_lng),Number(route.start_lat)],...(stops||[]).map((row:any)=>[Number(row.locations?.longitude),Number(row.locations?.latitude)] as [number,number])];if(points.some(([lng,lat])=>!Number.isFinite(lng)||!Number.isFinite(lat)))throw new Error('Saved route is missing valid coordinates.');const last:[number,number]=[Number(route.end_lng),Number(route.end_lat)];if(Number.isFinite(last[0])&&Number.isFinite(last[1])&&(points.at(-1)?.[0]!==last[0]||points.at(-1)?.[1]!==last[1]))points.push(last);if(points.length<2)throw new Error('Saved route needs at least two route points.');const response=await fetch(`https://router.project-osrm.org/route/v1/driving/${points.map(([lng,lat])=>`${lng},${lat}`).join(';')}?overview=full&geometries=geojson&steps=false`,{headers:{Accept:'application/json'}});if(!response.ok)throw new Error('Routing provider could not reconstruct this saved route.');const payload=await response.json();const geometry=payload?.routes?.[0]?.geometry;if(!geometry)throw new Error('Routing provider returned no route geometry.');const{data,error}=await c.rpc('set_route_plan_geometry',{p_route_id:routeId,p_route_geometry:geometry});if(error)throw error;return(data as any)?.route_geometry||geometry;}
export async function prepareRouteOfflinePack(routeId:string,name?:string){const c=client();await ensureRouteGeometry(routeId);const{data:session,error:sessionError}=await c.rpc('prepare_route_discovery',{p_route_id:routeId,p_corridor_meters:1000,p_expires_minutes:180});if(sessionError)throw sessionError;const sessionId=String((session as any)?.id||'');if(!sessionId)throw new Error('Route discovery did not return a session id.');const{count:discovered,error:countError}=await c.from('route_discovery_locations').select('location_id',{count:'exact',head:true}).eq('session_id',sessionId);if(countError)throw countError;if(!Number(discovered||0))throw new Error('No Kleenest restrooms were discovered inside this route corridor. The route was not marked offline-ready.');const{data:pack,error:packError}=await c.rpc('create_offline_pack',{p_pack_type:'route',p_name:name||'Offline route',p_business_id:null,p_route_discovery_session_id:sessionId,p_west:null,p_south:null,p_east:null,p_north:null,p_expires_hours:24});if(packError)throw packError;const packId=String((pack as any)?.id||'');const{count:packed,error:packedError}=packId?await c.from('offline_pack_locations').select('location_id',{count:'exact',head:true}).eq('pack_id',packId):{count:0,error:null};if(packedError)throw packedError;if(!Number(packed||0))throw new Error('Offline pack was created without restroom contents and was rejected as incomplete.');return{session,pack,discoveredLocations:Number(discovered||0),packedLocations:Number(packed||0)};}
