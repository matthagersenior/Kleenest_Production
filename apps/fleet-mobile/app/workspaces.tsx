import { router } from 'expo-router';
import { useEffect,useState } from 'react';
import { Pressable,RefreshControl,ScrollView,StyleSheet,Text,View } from 'react-native';
import { currentFleetBusinessId,listFleetWorkspaceOptions,selectFleetWorkspace } from '../services/control';
type Row=Record<string,any>;
export default function FleetWorkspaces(){const[rows,setRows]=useState<Row[]>([]),[selected,setSelected]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState('Loading Fleet workspaces…');async function load(){setBusy(true);try{const[data,id]=await Promise.all([listFleetWorkspaceOptions(),currentFleetBusinessId()]);setRows(data);setSelected(id);setMessage('')}catch(e:any){setMessage(e?.message||'Fleet workspaces unavailable.')}finally{setBusy(false)}}useEffect(()=>{void load()},[]);async function choose(id:string){setBusy(true);try{await selectFleetWorkspace(id);setSelected(id);setMessage('Fleet workspace selected.');router.replace('/')}catch(e:any){setMessage(e?.message||'Fleet workspace could not be selected.')}finally{setBusy(false)}}return <ScrollView refreshControl={<RefreshControl refreshing={busy} onRefresh={load}/>} contentInsetAdjustmentBehavior="automatic" contentContainerStyle={s.page}><View style={s.hero}><Text style={s.kicker}>FLEET WORKSPACE</Text><Text style={s.title}>Operate the right organization.</Text><Text style={s.body}>Only Fleet-authorized Business workspaces appear. Kleenest remembers the selection so dispatch, assets and operations stay aligned.</Text></View>{message?<Text accessibilityLiveRegion="polite" style={s.message}>{message}</Text>:null}{rows.map(row=>{const id=String(row.business_id||'');const active=id===selected;return <Pressable key={id} disabled={busy} onPress={()=>choose(id)} accessibilityRole="button" accessibilityState={{selected:active}} style={[s.card,active&&s.active]}><View style={{flex:1,gap:4}}><Text style={s.name}>{String(row.business_name||row.name||'Fleet workspace')}</Text><Text style={s.meta}>{String(row.role||'member').replaceAll('_',' ')} · {String(row.business_tier||'Fleet')}</Text></View><Text style={s.choice}>{active?'CURRENT':'USE'}</Text></Pressable>})}</ScrollView>}
const s=StyleSheet.create({page:{padding:18,gap:10,backgroundColor:'#f3f6f4',paddingBottom:60},hero:{backgroundColor:'#173f2d',borderRadius:24,padding:20,gap:7},kicker:{fontSize:10,fontWeight:'900',letterSpacing:1.4,color:'#c8ead7'},title:{fontSize:28,fontWeight:'900',color:'#fff'},body:{fontSize:14,lineHeight:21,color:'#deebe4'},message:{fontWeight:'700',color:'#596b61'},card:{minHeight:76,backgroundColor:'#fff',borderRadius:18,padding:15,borderWidth:1,borderColor:'#dbe5de',flexDirection:'row',alignItems:'center',gap:12},active:{borderWidth:2,borderColor:'#173f2d'},name:{fontSize:17,fontWeight:'900',color:'#102218'},meta:{fontSize:12,color:'#65756b'},choice:{fontSize:11,fontWeight:'900',color:'#173f2d'}});
