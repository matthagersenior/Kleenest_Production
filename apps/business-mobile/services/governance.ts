import { getKleenestSupabaseClient } from '@kleenest/mobile-core';
const client=()=>getKleenestSupabaseClient();async function rpc(name:string,args:Record<string,unknown>={}){const{data,error}=await client().rpc(name,args);if(error)throw error;return data;}
export async function listReportingSchedules(businessId:string){const{data,error}=await client().from('reporting_schedules').select('*').eq('scope_type','business').eq('scope_id',businessId).order('created_at',{ascending:false});if(error)throw error;return data||[];}
export async function createWeeklyBusinessReportSchedule(businessId:string,name='Weekly Business Report'){const{data:{user},error:userError}=await client().auth.getUser();if(userError)throw userError;if(!user)throw new Error('Authentication required.');const{data,error}=await client().from('reporting_schedules').insert({owner_id:user.id,scope_type:'business',scope_id:businessId,name,cadence:'weekly',day_of_week:1,hour_local:8,timezone:'America/Chicago',metrics:[],recipients:[],enabled:true}).select('*').single();if(error)throw error;return data;}
export async function setReportingScheduleEnabled(scheduleId:string,enabled:boolean){const{data,error}=await client().from('reporting_schedules').update({enabled,updated_at:new Date().toISOString()}).eq('id',scheduleId).select('*').single();if(error)throw error;return data;}
export async function deleteReportingSchedule(scheduleId:string){const{error}=await client().from('reporting_schedules').delete().eq('id',scheduleId);if(error)throw error;return true;}
export async function runDueReportingSchedules(){const{data:{user},error:userError}=await client().auth.getUser();if(userError)throw userError;if(!user)throw new Error('Authentication required.');return rpc('run_due_reporting_schedules',{p_requested_by:user.id});}
export async function buildGovernanceReport(businessId:string,days=30){const end=new Date(),start=new Date(end.getTime()-days*86400000);return rpc('reporting_build_payload',{p_scope_type:'business',p_scope_id:businessId,p_start:start.toISOString(),p_end:end.toISOString()});}
