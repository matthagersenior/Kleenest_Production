import fs from 'node:fs';
const required=['supabase/migrations/20260830221000_native_notification_push_transport.sql','supabase/functions/deliver-native-push-notification/index.ts','apps/consumer-mobile/services/push.ts','apps/consumer-mobile/services/notificationRouting.ts','apps/consumer-mobile/app/notifications.tsx','apps/consumer-mobile/app/_layout.tsx','apps/consumer-mobile/app.config.ts','apps/consumer-mobile/package.json'];
const failures=[];for(const file of required)if(!fs.existsSync(file))failures.push(`missing native push authority file: ${file}`);const read=file=>fs.readFileSync(file,'utf8');
if(!failures.length){const migration=read(required[0]),worker=read(required[1]),service=read(required[2]),routing=read(required[3]),screen=read(required[4]),layout=read(required[5]),config=read(required[6]),pkg=read(required[7]);const checks=[[migration.includes('notification_native_push_tokens')&&migration.includes('notification_native_push_deliveries'),'native push must own distinct token and delivery tables'],[migration.includes('register_notification_native_push_token')&&migration.includes('remove_notification_native_push_token'),'native token lifecycle must be authenticated RPC authority'],[migration.includes('notifications_native_push_delivery')&&migration.includes('deliver-native-push-notification'),'notifications must fan out to native worker'],[worker.includes('https://exp.host/--/api/v2/push/send'),'native worker must use Expo Push Service'],[worker.includes('DeviceNotRegistered')&&worker.includes("active:false"),'native worker must retire invalid devices'],[worker.includes('notification_id:notification.id')&&worker.includes('type:notification.type'),'native push payload must preserve notification context'],[service.includes('getExpoPushTokenAsync')&&service.includes("rpc('register_notification_native_push_token'"),'mobile client must obtain and register Expo tokens'],[!service.includes('notification_push_subscriptions'),'mobile push must never write browser PushManager subscriptions'],[routing.includes('location_id')&&routing.includes('/location/')&&routing.includes('/contributor/')&&routing.includes("'/games'")&&routing.includes("'/play'"),'notification routing must resolve bathroom, contributor, game, and progression destinations'],[screen.includes('notificationDestination')&&screen.includes('markMobileNotificationRead')&&screen.includes('router.push'),'notification center must mark updates read and open their canonical context'],[screen.includes('registerNativePush'),'notification center must expose device push registration'],[layout.includes('setNotificationHandler')&&layout.includes('shouldShowBanner'),'foreground notifications must have explicit presentation handling'],[layout.includes('getLastNotificationResponseAsync')&&layout.includes('addNotificationResponseReceivedListener')&&layout.includes('notificationDestination'),'cold-start and foreground/background push taps must deep-link through the same routing authority'],[pkg.includes('"expo-notifications": "~57.0.15"'),'Expo SDK 57 notification dependency must remain compatible'],[config.includes("'expo-notifications'")&&config.includes("defaultChannel: 'kleenest-updates'"),'native config must include notification plugin/channel']];for(const[ok,msg]of checks)if(!ok)failures.push(msg)}
if(failures.length){console.error('Native push authority audit failed:');for(const failure of failures)console.error(`- ${failure}`);process.exit(1)}console.log('Native push authority audit passed.');
