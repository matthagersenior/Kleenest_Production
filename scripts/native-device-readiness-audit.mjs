import fs from 'node:fs';
const failures=[];
const required=['apps/consumer-mobile/app.config.ts','apps/consumer-mobile/eas.json','apps/consumer-mobile/package.json','apps/consumer-mobile/app/explore.tsx','apps/consumer-mobile/app/qr.tsx','apps/consumer-mobile/app/notifications.tsx','apps/consumer-mobile/app/live-network.tsx','apps/consumer-mobile/services/liveNetwork.ts','.github/workflows/android-preview.yml','.github/workflows/eas-android-build.yml'];
for(const file of required)if(!fs.existsSync(file))failures.push(`missing device-readiness file: ${file}`);
if(!failures.length){
 const config=fs.readFileSync(required[0],'utf8'),eas=JSON.parse(fs.readFileSync(required[1],'utf8')),pkg=JSON.parse(fs.readFileSync(required[2],'utf8')),explore=fs.readFileSync(required[3],'utf8'),qr=fs.readFileSync(required[4],'utf8'),notifications=fs.readFileSync(required[5],'utf8'),liveNetwork=fs.readFileSync(required[6],'utf8'),liveNetworkService=fs.readFileSync(required[7],'utf8'),androidStandalone=fs.readFileSync(required[8],'utf8'),easCloudBuild=fs.readFileSync(required[9],'utf8');
 const projectId='22a65aa3-c615-4c4f-a34d-084babc28fd7';
 for(const token of ["bundleIdentifier: 'com.kleenest.app'","package: 'com.kleenest.app'",projectId,"ACCESS_COARSE_LOCATION","ACCESS_FINE_LOCATION","ACCESS_BACKGROUND_LOCATION","CAMERA","expo-camera","expo-notifications","expo-dev-client","defaultChannel: 'kleenest-updates'","userInterfaceStyle: 'automatic'","blockedPermissions","android.permission.RECORD_AUDIO","android.permission.SYSTEM_ALERT_WINDOW","isAndroidBackgroundLocationEnabled: true","isIosBackgroundLocationEnabled: true"])if(!config.includes(token))failures.push(`native config missing ${token}`);
 if(eas?.cli?.appVersionSource!=='remote')failures.push('EAS must use remote app versioning.');
 if(eas?.build?.development?.android?.buildType!=='apk'||eas?.build?.development?.developmentClient!==true||eas?.build?.development?.autoIncrement!==true)failures.push('Development Android profile must remain available for developer-only builds.');
 if(eas?.build?.preview?.android?.buildType!=='apk'||eas?.build?.preview?.distribution!=='internal'||eas?.build?.preview?.autoIncrement!==true)failures.push('Preview Android profile must produce an internally distributed standalone APK.');
 if(eas?.build?.production?.android?.buildType!=='app-bundle'||eas?.build?.production?.autoIncrement!==true)failures.push('Production Android profile must produce an auto-incremented app bundle.');
 for(const profile of ['development','preview','production'])if(eas?.build?.[profile]?.env?.EAS_PROJECT_ID!==projectId)failures.push(`${profile} EAS profile must bind the canonical Expo project id.`);
 if(!(pkg.dependencies?.['expo-dev-client']||pkg.devDependencies?.['expo-dev-client']))failures.push('consumer mobile dependency missing expo-dev-client');
 for(const dep of ['expo-location','expo-camera','expo-notifications','expo-task-manager','expo-secure-store','@maplibre/maplibre-react-native','react-native-safe-area-context'])if(!pkg.dependencies?.[dep])failures.push(`consumer mobile dependency missing ${dep}`);
 for(const script of ['start:dev-client','prebuild:android','eas:development:android','eas:preview:android','eas:production:android'])if(!pkg.scripts?.[script])failures.push(`consumer mobile script missing ${script}`);
 if(!explore.includes('requestForegroundPermissionsAsync')||!explore.includes('RefreshControl')||!explore.includes('Live lookup failed. Showing cached bathrooms'))failures.push('Explore must support runtime location permission, refresh, and cached recovery on device.');
 if(!qr.includes('useCameraPermissions')||!qr.includes('CameraView')||!qr.includes("barcodeTypes:['qr']")||!qr.includes('AppState.addEventListener')||!qr.includes('Linking.openSettings'))failures.push('QR must use native camera permission, background-safe lifecycle, settings recovery, and QR-only scanning.');
 if(!notifications.includes('registerNativePush')||!notifications.includes('RefreshControl'))failures.push('Notifications must keep native push registration and inbox refresh behavior.');
 for(const token of ['Live Network','enableConsumerLiveNetwork','disableConsumerLiveNetwork','getConsumerLiveNetworkStatus'])if(!liveNetwork.includes(token))failures.push(`Consumer Live Network UI missing ${token}`);
 for(const token of ['expo-location','expo-task-manager','startGeofencingAsync','create_gps_geofence_notification','map_network_nearby_v2','register_notification_native_push_token','consumer-live-network'])if(!liveNetworkService.includes(token))failures.push(`Consumer Live Network service missing ${token}`);
 // Standalone builds are Gradle release builds. Validate the current workspace-aware
 // workflow rather than legacy flat-repo filenames that no longer exist.
 for(const token of ['Build Consumer Standalone Android APK','workflow_dispatch:','push:','KLEENEST_STANDALONE_ANDROID: 1','npm run native:typecheck','native-consumer-apk-convergence-audit.mjs','native-consumer-presentation-convergence-audit.mjs','npx expo prebuild --platform android','assembleRelease','actions/upload-artifact@v4','Kleenest-Consumer-Standalone-APK',projectId])if(!androidStandalone.includes(token))failures.push(`Android standalone workflow missing ${token}`);
 for(const forbidden of ['./gradlew assembleDebug','app-debug.apk'])if(androidStandalone.includes(forbidden))failures.push(`Android standalone workflow must not build ${forbidden}`);
 for(const token of ['workflow_dispatch:','development','preview','production','secrets.EXPO_TOKEN','Require Expo automation token','production-environment-contract-audit.mjs','native-device-readiness-audit.mjs','eas-cli@latest build','--platform android','--non-interactive','--wait','--json','eas-build-result.json','Validate EAS build result','actions/upload-artifact@v4',projectId])if(!easCloudBuild.includes(token))failures.push(`EAS Android cloud-build workflow missing ${token}`);
 if(easCloudBuild.includes("- '.github/workflows/eas-android-build.yml'"))failures.push('EAS workflow must not self-trigger on workflow-only maintenance changes.');
 if(/service_role/i.test(config+explore+qr+notifications+liveNetwork+liveNetworkService+androidStandalone+easCloudBuild))failures.push('Device surfaces and build workflows must never contain service-role credentials.');
}
if(failures.length){console.error('Native device readiness audit failed:');for(const failure of failures)console.error(`- ${failure}`);process.exit(1)}
console.log('Native device readiness audit passed.');
